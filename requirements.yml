name: Opt_UKESM1
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - numpy==2.2 # stop cf-units updating it
  - cf-units
  - ipython
  - pip: # install what we can with pip
    - -r requirements.txt
